// Temporarily disable auto-hide for navigation buttons, due to a bug where they still reserve space in hidden state https://github.com/black7375/Firefox-UI-Fix/issues/1178
lockPref('userChrome.autohide.back_button', false);
lockPref('userChrome.autohide.forward_button', false);
